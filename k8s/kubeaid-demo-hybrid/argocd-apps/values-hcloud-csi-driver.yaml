hcloud-csi:
  controller:
    hcloudToken:
      existingSecret:
        name: cloud-credentials
        key: hcloud

    nodeSelector:
      node.cluster.x-k8s.io/cloud-provider: hcloud

  node:
    nodeSelector:
      node.cluster.x-k8s.io/cloud-provider: hcloud
